import { ScannerConfig } from './scanner-configs.model';

describe('ScannerConfigs', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ScannerConfig()).toBeTruthy();
  });
});
